# Example secrets file
# Copy this to secrets.yaml and encrypt with: sops -e -i secrets.yaml
#
# Usage:
# 1. Generate age key: mkdir -p ~/.config/sops/age && age-keygen -o ~/.config/sops/age/keys.txt
# 2. Get public key: age-keygen -y ~/.config/sops/age/keys.txt
# 3. Update .sops.yaml with your public key
# 4. Copy this file: cp secrets.yaml.example secrets.yaml
# 5. Encrypt: sops -e -i secrets.yaml
# 6. Edit encrypted file: sops secrets.yaml

# API Keys
github_token: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
openai_api_key: sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Database credentials
db_password: supersecretpassword

# SSH private key (can store entire key)
ssh_private_key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  your key here
  -----END OPENSSH PRIVATE KEY-----
